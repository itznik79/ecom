export async function seedPermissions() {
  const permissions = [
    "user.create",
    "user.update",
    "user.delete",
    "role.manage",
    "permission.manage",
  ];

  // insert ignore if exists
}
